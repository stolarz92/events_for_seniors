%h2
  Najnowsze wydarzenia w mieście #{@city.name}
- @newest.each do |event|
  .col-xs-12.col-sm-6.col-md-3.col-lg-3.event-item
    %h4.event-name= event.name
    = link_to city_event_path(@city, event) do
      = image_tag event.image(:thumb)
