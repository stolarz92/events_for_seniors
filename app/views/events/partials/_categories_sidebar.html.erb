<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">Kategorie</h3>
  </div>
  <div class="panel-body">
    <ul>
      <% @categories.each do |category| %>
          <li class="<%=  "category-active" if category.id == params[:category_id] %>">
            <%= link_to category.name, events_path(
                                             :search => params[:search],
                                             :start_date => params[:start_date],
                                             :category_id => category.id,
                                             :effort => params[:effort],
                                             :cost => params[:cost]
                                     )
            %></li>
      <% end %>
    </ul>
  </div>
</div>

<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">Wysiłek</h3>
  </div>
  <div class="panel-body">
    <ul>
      <% effort.each do |key, value| %>
          <li class="<%=  "category-active" if effort == params[:effort] %>">
            <%= link_to key, events_path(
                                          :search => params[:search],
                                          :start_date => params[:start_date],
                                          :category_id => params[:category_id],
                                          :effort => value,
                                          :cost => params[:cost]
                                  )
            %></li>
      <% end %>
    </ul>
  </div>
</div>

<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">Koszt</h3>
  </div>
  <div class="panel-body">
    <ul>
      <% cost.each do |key, value| %>
          <li class="<%=  "category-active" if cost == params[:cost] %>">
            <%= link_to key, events_path(
                                          :search => params[:search],
                                          :start_date => params[:start_date],
                                          :category_id => params[:category_id],
                                          :effort => value,
                                          :cost => params[:cost]
                                  )
            %></li>
      <% end %>
    </ul>
  </div>
</div>