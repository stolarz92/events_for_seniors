<%= form_for(current_user.event_relationships.build, remote: true) do |f| %>
    <div><%= hidden_field_tag :event_id, @event.id %></div>
    <%= f.submit "Zapisz do kalendarza", class: "btn btn-success" %>
<% end %>