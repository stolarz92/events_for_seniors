= form_for(current_user.event_relationships.find_by(event_id: @event.id), |
           html: { method: :delete },                                     |
           remote: true                                                   |
  ) do |f|                                                                |
  = f.submit "Nie jestem zainteresowany/a", class: "btn"
